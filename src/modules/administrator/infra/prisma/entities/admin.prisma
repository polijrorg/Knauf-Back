model Administrator {
    id          String      @id @default(uuid())
    email       String
    name        String
    password    String
    language    String
    created_at  DateTime     @default(now())
}